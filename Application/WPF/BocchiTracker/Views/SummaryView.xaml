<UserControl x:Class="BocchiTracker.Views.SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BocchiTracker.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d" 
             d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.25*"/>
            <ColumnDefinition Width="0.8*"/>
        </Grid.ColumnDefinitions>

        <ComboBox ItemsSource="{Binding TicketTypes}" 
                  SelectedItem="{Binding SelectedTicketType.Value}" 
                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                  materialDesign:HintAssist.Hint="Select TicketType"
                  FontSize="14"
                  Foreground="White"
                  Grid.Column="0" Margin="5,0,5,0" />
        <TextBox Grid.Column="1"
                 Text="{Binding Summary.Value}"
                 materialDesign:HintAssist.Hint="Summary..."
                 Foreground="White"
                 Margin="10, 0, 5, 0" TextWrapping="NoWrap" FontSize="16" VerticalContentAlignment="Bottom" />
    </Grid>
</UserControl>
