<UserControl x:Class="BocchiTracker.Views.TicketDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:customUI="clr-namespace:BocchiTracker.CustomControl"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <customUI:OneChoice ViewModel="{Binding ConnectTo}"/>
        <customUI:OneChoice ViewModel="{Binding TicketAssign}"/>
        <customUI:OneChoice ViewModel="{Binding TicketClass}"/>
        <customUI:OneChoice ViewModel="{Binding TicketPriority}"/>
        <customUI:MultipleChoice ViewModel="{Binding TicketLabels}"/>
        <customUI:MultipleChoice ViewModel="{Binding TicketWatchers}"/>
    </StackPanel>
</UserControl>
