<UserControl x:Class="BocchiTracker.Client.Views.TicketDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:BocchiTracker.Client.Controls"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <controls:OneChoice ViewModel="{Binding ConnectTo}"/>
        <controls:OneChoice ViewModel="{Binding TicketAssign}"/>
        <controls:OneChoice ViewModel="{Binding TicketClass}"/>
        <controls:OneChoice ViewModel="{Binding TicketPriority}"/>
        <controls:MultipleChoice ViewModel="{Binding TicketLabels}"/>
        <controls:MultipleChoice ViewModel="{Binding TicketWatchers}"/>
    </StackPanel>
</UserControl>
